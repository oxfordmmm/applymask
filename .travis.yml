language: python

python:
 - "3.7"

before_install:
 - pip install coverage
 - pip install codecov 

install:
 - pip install -r requirements.txt

script:
 - coverage run -m unittest discover -s . -p test_*.py

after_success:
 - codecov
